function numero() {
    return Math.floor(Math.random() * 100);
    //o return vai ser atribuído ao nome da função: numero//
    //os parâmetros de números são vazios (void)//
};

// numero() //Chamando a função para ser executada//
// console.log(numero());

function aleatorio() {

    let megaSena = [];
    let numeros = 1;

    while(numero <= 6) {
        
    };
};